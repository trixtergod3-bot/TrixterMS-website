# Beta portal delivery report

Date: 2026-09-12. Scope: website preparation and publication, not game deployment or domain cutover. Exact checkpoint and final handoff revision are recorded in docs/coordination/chats/web-portal-beta-01a096f8.json and Git history.

1. **Existing source found:** `C:/Users/Studio PC/Documents/ChatGPT/TrixterMS/local/worktrees/trixterms-website-v2/website`, clean Website V2 at `3b59bf05d78750c8a3cb14d871aeda448d3f6ac4`. See SOURCE_MIGRATION.md for the other three candidates and access caveats.
2. **Framework:** React 19.2.8, Next.js 16.3.3 App Router, TypeScript, custom responsive CSS, Lucide. Vinext/Cloudflare/Sites deployment coupling was replaced by a standard Node production build. Playwright checks the production browser output.
3. **Existing V2 capability:** 20 portal routes, ranking/profile components, typed provider and models, algorithm tests, labeled synthetic fixtures, and a localhost-only status/ranking/profile adapter. That local adapter was not a production backend.
4. **Preservation:** All original source worktrees and their art/history remain untouched. Reused V2 models, tests, component foundations and CSS remain in this repository. Historical local runtime audits and old artwork were excluded; no game history, proprietary binaries, database, private configuration or runtime logs were imported.
5. **Implemented pages:** `/`, `/download`, `/register`, `/rankings`, `/rankings/daily`, `/rankings/weekly`, `/achievements`, `/database`, `/free-market`, `/vote`, `/donate`, `/discord`; plus `/news`, `/character/:name`, `/status`, `/classes`, `/features`, `/guide`, `/patch-notes`, `/players` and prepared event/record pages. Legacy daily/community links redirect.
6. **Functional features:** responsive navigation, original hero, visible unknown-safe status, ranking period/class/fame filters, historical competition forms, achievement search/categories, character telemetry/progress presentation, six database category shells, FM search/category/pagination, registration validation/gateway, download metadata validation, and bounded public API proxies. Search shells retain user query even when data is unavailable.
7. **Fixtures/flags:** no synthetic player data is activated in production. Reused fixtures are isolated automated-test inputs. The 40-definition achievement preview is actual staged source catalog metadata, not fabricated unlocks. Live data defaults unavailable. Daily/weekly flags default off. Registration, download links, Discord, voting and checkout require reviewed configuration; no provider/invite/download was invented. Homepage global recent unlocks is an honest unavailable placeholder, with future global-feed read model still required.
8. **Backend requirements:** 11 same-origin public GET contracts proxy only a reviewed HTTPS read backend. MariaDB remains private behind least-privilege queries/views. Runtime status must be observed, not derived from database login flags. Registration needs an authenticated private service matching AccountRegistrationService validation and salted SHA-512 compatibility, uniqueness/normal-player defaults and distributed abuse controls. No account was created by this task.
9. **Telemetry readiness:** source contains 12 lifetime/daily metric keys. Public values are decimal strings and nullable; total dimension is not added to its subdimensions. Actual configured timezone, partial economy coverage and eligibility limitations are documented. No live beta telemetry was consumed.
10. **Ranking readiness:** level/EXP/fame/job UI and contracts ready; backend must preserve staff/banned/minimum-level eligibility. Daily UI exposes the exact 11 specification categories and top-100/winner/history structure. Finalization, tie-break evidence, reward engine and weekly read model are not claimed implemented. Weekly proposal is documented.
11. **Achievement readiness:** staged catalog version2 has 40 definitions, 8 categories and 2,160 AP. Live character progress requires matching catalog version, keys, thresholds, AP and consistent unlock records. Broader 500-definition draft is future scope; activation and reward delivery are not inferred.
12. **FM/database readiness:** FM viewer is for read-only NPC shops, not player merchant listings. Currency/rules and nullable stock are prepared from the inspected schema. Item/equipment/monster/boss APIs require an approved bounded export; maps/skills have searchable unavailable shells. No WZ-derived catalog/assets were published.
13. **Visual asset brief:** `docs/TRIXTERMS_WEB_VISUAL_ASSET_BRIEF.md` defines 11 slots including separate Explorer and Demon Avenger promotion, dimensions, ratios, transparency, desktop/mobile crop behavior and placement. New original hero derivative is 1672x941, 240,778 bytes. Prompt and hashes are in ART_PROVENANCE.md. Other painted assets remain reserved slots.
14. **Git branch/commit:** the original migration is preserved on `codex/web-portal-beta-v1`. The subsequent owner-authorized publication uses **`main`**. See [publication readiness](GITHUB_PUBLISH_HOSTINGER_READINESS.md) and its lane JSON for the verified snapshot. Source contains only website-owned files.
15. **Dedicated repository:** `trixtergod3-bot/TrixterMS-website` was empty before migration and is now populated. Main was created from the frozen reviewed migration and handoff snapshot; hosting remains unconnected by this work.
16. **Hostinger blocker:** account/plan/Git authorization, actual Node app settings, TLS/domain mapping and preservation of the existing `/beta` feed are not verified. Managed Node deployment may replace routing. Build command `npm run build`, start `npm start`, root directory repository root; configure Node22.13+ or24 LTS and platform PORT. Do not point source files at static/PHP Git hosting.
17. **Next milestone:** integrate a private read-only beta API beginning with fresh status, filtered character rankings/profiles and the staged achievement catalog; integrate the guarded registration gateway; build a Hostinger preview from the reviewed SHA and prove `/beta` remains intact. Owner visual acceptance and live native-login/package QA are separate gates before public release.

Named checks and their observed outcomes are in VERIFICATION.md. A locally verified portal does not establish live game integration, owner acceptance, published client availability or deployment.
