{
  "name": "trixterms-public-telemetry-bridge",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22.13.0 <25" },
  "scripts": {
    "start": "node --env-file=local/bridge.env src/main.mjs",
    "test": "node --test tests/*.test.mjs",
    "validate:database": "node --env-file=local/bridge.env src/validate-database.mjs"
  },
  "dependencies": { "mariadb": "3.5.4" }
}
